# 学工系统认证 （脱敏版）

### 

## 1.离校管理接口分析

### 1.1 基础信息

| 字段        | 内容               |
| --------- | ---------------- |
| **请求URL** | `leaveSchool.do` |
| **请求方法**  | `POST`           |
| **请求类型**  | `XHR`            |
| **状态码**   | `200`            |

### 1.2  请求参数

#### a. URL Query 参数

| 参数名                  | 参数值 | 说明         |
| -------------------- | --- | ---------- |
| （可在Payload标签页查看完整参数） |     | 离校业务相关请求参数 |

#### b.Form Data 参数

| 参数名                    | 参数值 | 说明            |
| ---------------------- | --- | ------------- |
| （可在Payload标签页查看完整表单参数） |     | 离校管理提交/查询相关字段 |

### 1.3接口说明

- **`leaveSchool.do`**：离校管理业务接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 属于学校统一离校办理流程中的核心接口，可用于查询离校办理进度或提交离校申请



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/leaveSchool.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "离校管理页面地址"
 }
)
```




 

## 2. 综合测评接口分析

### 2.1 基础信息

| 字段        | 内容               |
| --------- | ---------------- |
| **请求URL** | `zhcptybbapp.do` |
| **请求方法**  | `POST`           |
| **请求类型**  | `XHR`            |
| **状态码**   | `200`            |

### 2.2 请求参数

#### a. URL Query 参数

| 参数名                  | 参数值 | 说明           |
| -------------------- | --- | ------------ |
| （可在Payload标签页查看完整参数） |     | 综合测评业务相关请求参数 |

#### b.Form Data 参数

| 参数名                    | 参数值 | 说明            |
| ---------------------- | --- | ------------- |
| （可在Payload标签页查看完整表单参数） |     | 综合测评查询/提交相关字段 |

### 2.3 接口说明

- **`zhcptybbapp.do`**：综合测评业务接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称由“综合素质测评”拼音缩写构成，是学校综合素质测评系统的核心接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于查询测评进度、提交测评数据或获取测评结果



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/zhcptybbapp.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "综合测评页面地址"
 }
)
```




 

# 3. 奖学金接口分析

| 字段        | 内容         |
| --------- | ---------- |
| **请求URL** | `apply.do` |
| **请求方法**  | `POST`     |
| **请求类型**  | `XHR`      |
| **状态码**   | `200`      |

### 3.1 请求参数

#### a.URL Query 参数

| 参数名                  | 参数值 | 说明               |
| -------------------- | --- | ---------------- |
| （可在Payload标签页查看完整参数） |     | 奖学金申请/查询业务相关请求参数 |

#### b.Form Data 参数

| 参数名                    | 参数值 | 说明             |
| ---------------------- | --- | -------------- |
| （可在Payload标签页查看完整表单参数） |     | 奖学金申请提交/查询相关字段 |

### 3.2 接口说明

- **`apply.do`**：奖学金申请/查询核心接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称 `apply` 直接对应“申请”操作，是奖学金系统的关键业务接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于获取奖学金申请列表、提交申请材料或查询申请进度



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/apply.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "奖学金申请页面地址"
 }
)
```






## 4.违纪处分接口分析

### 4.1基础信息

| 字段        | 内容                     |
| --------- | ---------------------- |
| **请求URL** | `getViolateExamine.do` |
| **请求方法**  | `POST`                 |
| **请求类型**  | `XHR`                  |
| **状态码**   | `200`                  |

### 4.3请求参数

#### a. URL Query 参数

| 参数名                  | 参数值 | 说明           |
| -------------------- | --- | ------------ |
| （可在Payload标签页查看完整参数） |     | 违纪处分业务相关请求参数 |

#### b.Form Data 参数

| 参数名                    | 参数值 | 说明         |
| ---------------------- | --- | ---------- |
| （可在Payload标签页查看完整表单参数） |     | 违纪处分查询相关字段 |

### 接口说明

- **`getViolateExamine.do`**：违纪处分查询接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称由“违纪审查”拼音/英文缩写构成，是违纪处分模块的核心查询接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于查询学生违纪处分记录、处分详情等信息



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/getViolateExamine.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "违纪处分查询页面地址"
 }
)
```

 




## 5. 助学金接口分析

### 5.1 基础信息

| 字段        | 内容         |
| --------- | ---------- |
| **请求URL** | `apply.do` |
| **请求方法**  | `POST`     |
| **请求类型**  | `XHR`      |
| **状态码**   | `200`      |

### 5.2 请求参数

#### a.URL Query 参数

| 参数名                  | 参数值 | 说明               |
| -------------------- | --- | ---------------- |
| （可在Payload标签页查看完整参数） |     | 助学金申请/查询业务相关请求参数 |

#### b. Form Data 参数

| 参数名                    | 参数值 | 说明             |
| ---------------------- | --- | -------------- |
| （可在Payload标签页查看完整表单参数） |     | 助学金申请提交/查询相关字段 |

### 5.3接口说明

- **`apply.do`**：助学金申请/查询核心接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称 `apply` 直接对应“申请”操作，是助学金系统的关键业务接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于获取助学金申请列表、提交申请材料或查询申请进度



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/apply.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "助学金申请页面地址"
 }
)
```






## 6.节假日去向接口分析

| 字段        | 内容             |
| --------- | -------------- |
| **请求URL** | `jjrlfxapp.do` |
| **请求方法**  | `POST`         |
| **请求类型**  | `XHR`          |
| **状态码**   | `200`          |

### 6.1 请求参数

#### a.URL Query 参数

| 参数名                  | 参数值 | 说明            |
| -------------------- | --- | ------------- |
| （可在Payload标签页查看完整参数） |     | 节假日去向业务相关请求参数 |

#### b. Form Data 参数

| 参数名                    | 参数值 | 说明             |
| ---------------------- | --- | -------------- |
| （可在Payload标签页查看完整表单参数） |     | 节假日去向登记/查询相关字段 |

### 6.2 接口说明

- **`jjrlfxapp.do`**：节假日去向登记接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称由“节假日离校去向”拼音缩写构成，是节假日去向登记系统的核心业务接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于提交节假日去向信息、查询历史去向记录或获取登记页面数据



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/jjrlfxapp.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "节假日去向登记页面地址"
 }
)


```






## 7. 第二课堂接口分析

| 字段        | 内容        |
| --------- | --------- |
| **请求URL** | `hdsb.do` |
| **请求方法**  | `POST`    |
| **请求类型**  | `XHR`     |
| **状态码**   | `200`     |

### 7.1请求参数

#### a.URL Query 参数

| 参数名                  | 参数值 | 说明           |
| -------------------- | --- | ------------ |
| （可在Payload标签页查看完整参数） |     | 第二课堂业务相关请求参数 |

#### b.Form Data 参数

| 参数名                    | 参数值 | 说明            |
| ---------------------- | --- | ------------- |
| （可在Payload标签页查看完整表单参数） |     | 第二课堂报名/查询相关字段 |

### 

### 7.2 接口说明

- **`hdsb.do`**：第二课堂活动报名/查询接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称由“活动申报”拼音缩写构成，是第二课堂系统的核心业务接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于获取第二课堂活动列表、提交活动报名或查询参与记录



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/hdsb.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "第二课堂活动页面地址"
 }
)

```






## 8.请假接口分析

### 8.1基础信息

| 字段        | 内容        |
| --------- | --------- |
| **请求URL** | `wdqj.do` |
| **请求方法**  | `POST`    |
| **请求类型**  | `XHR`     |
| **状态码**   | `200`     |

### 8.2 请求参数

#### a.URL Query 参数

| 参数名                  | 参数值 | 说明         |
| -------------------- | --- | ---------- |
| （可在Payload标签页查看完整参数） |     | 请假业务相关请求参数 |

#### b.Form Data 参数

| 参数名                    | 参数值 | 说明            |
| ---------------------- | --- | ------------- |
| （可在Payload标签页查看完整表单参数） |     | 请假申请提交/查询相关字段 |

### 8.3 接口说明

- **`wdqj.do`**：请假申请/查询核心接口，采用 `.do` 后缀的典型Java Web后端格式
- 接口名称由“请假（外出行/外出请假）”拼音缩写构成，是请假系统的关键业务接口
- 接口依赖登录 Cookie，需携带用户身份标识（如 `USERID`）才能正常访问
- 可用于提交请假申请、获取请假列表、查询请假审批状态等



### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/wdqj.do"

# 请求参数（需补充实际的URL参数和表单数据）

params = {

# 从DevTools的URL参数中复制完整内容

}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "请假申请页面地址"
 }
)

###
```



