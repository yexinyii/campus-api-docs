# 校园教务系统认证 （脱敏版）

## 1.课程相关接口分析

### 1.1 基础信息

| 项目   | 说明                                               |
|:---- |:------------------------------------------------ |
| 接口名称 | 课程模块入口页                                          |
| 请求方式 | GET                                              |
| 请求地址 | `/index_cxYhxxIndex.html`                        |
| 请求参数 | `xt=jw`、`localeKey=zh_CN`、`_=时间戳`、`gnmkdm=index` |
| 接口用途 | 课表/成绩等教务模块的主入口，加载课程相关页面                          |

#### 参数分析

- `xt=jw`：教务系统模块标识，表明这是教务（JW）相关页面
- `localeKey=zh_CN`：多语言标识，适配中文环境
- `_=时间戳`：防缓存参数，避免页面缓存导致教务数据不更新
- `gnmkdm=index`：模块/菜单标识，定位到首页入口

## 

## 2 教务详情页面接口

### 2.1 基础信息

| 项目   | 说明                               |
|:---- |:-------------------------------- |
| 接口名称 | 教务详情页面                           |
| 请求方式 | GET                              |
| 请求地址 | `/index_cxWdyy.html`             |
| 请求参数 | `localeKey=zh_CN`、`gnmkdm=index` |
| 接口用途 | 课程/成绩详情页面入口，加载具体教务业务             |

#### 参数分析

- `localeKey=zh_CN`：语言标识，保持与系统多语言逻辑一致
- `gnmkdm=index`：模块标识，定位到对应功能入口

## 

## 3 全局配置文件接口

### 3.1 基础信息

| 项目   | 说明                             |
|:---- |:------------------------------ |
| 接口名称 | 系统全局配置文件                       |
| 请求方式 | GET                            |
| 请求地址 | `/default_config.content.json` |
| 请求参数 | 无                              |
| 接口用途 | 加载系统全局配置，包含多语言、模块开关、主题配置等      |

## 整体技术特征分析

1. **模块化命名规范**
   - 接口名称采用 `index_cx业务缩写.html` 格式，`cx` 通常代表“查询”，如 `cxYhxx`（学生相关）、`cxWdyy`（我的业务），是典型的校园教务系统命名风格。
2. **多语言统一适配**
   - 所有教务相关接口均携带 `localeKey=zh_CN` 参数，与门户接口的多语言逻辑保持一致，验证了系统整体采用统一的 i18n 国际化方案。
3. **防缓存机制延续**
   - 关键页面请求携带 `_=时间戳` 参数，与门户接口的 `t` 参数功能一致，说明全站采用统一的防缓存策略。
4. **前后端分离架构验证**
   - 页面请求为静态 HTML 文件，数据通过后续接口异步加载，是典型的 Vue SPA 教务系统架构，与前面分析的门户技术栈完全吻合。

---

---

## 4.核心查询接口

### 4.1基础信息

| 项目   | 说明                       |
|:---- |:------------------------ |
| 接口名称 | 教务数据查询接口                 |
| 请求方式 | GET/POST（XHR）            |
| 请求地址 | `/xjy dsq_cxXjydsq.html` |
| 请求参数 | `doType=query` 等         |
| 状态码  | 200                      |
| 发起方  | `jquery.min.js`          |
| 接口用途 | 执行教务业务数据查询，返回结构化数据       |

### 4.2 技术特征

- 请求类型为 `XHR`，由 jQuery 发起，说明该模块使用 jQuery 实现异步请求
- 状态码为 `200`，接口响应正常，数据返回成功
- 响应体大小仅 `0.7 kB`，说明返回数据为精简的结构化 JSON 结果

## 

## 5. 学籍异动系统全局配置文件接口

### 5.1 基础信息

| 项目   | 说明                             |
|:---- |:------------------------------ |
| 接口名称 | 系统全局配置文件                       |
| 请求方式 | Fetch                          |
| 请求地址 | `/default_config.content.json` |
| 状态码  | 200                            |
| 发起方  | `content_main.js`              |
| 接口用途 | 加载系统全局配置，包含多语言、模块开关、主题配置等      |

### 5.2 技术特征

- 响应体大小为 `143 kB`，说明配置文件内容丰富，包含大量全局配置信息
- 发起方为 `content_main.js`，由主业务脚本在页面初始化阶段加载
- 请求类型为 `Fetch`，与 jQuery 接口的异步请求方式形成对比，说明系统存在多种异步请求方案

## 

## 6. 辅修专业报名模块接口分析

### 6.1 基础信息

| 项目   | 说明                             |
|:---- |:------------------------------ |
| 接口名称 | 系统全局配置文件                       |
| 请求方式 | Fetch                          |
| 请求地址 | `/default_config.content.json` |
| 状态码  | 200                            |
| 发起方  | `content_main.js`              |
| 响应大小 | 143 kB                         |
| 耗时   | 2 ms                           |
| 接口用途 | 加载辅修报名模块的全局配置、功能开关、多语言文案、表单规则等 |

### 6.2 技术分析

- 接口由 `content_main.js` 在页面初始化阶段通过 Fetch API 异步加载，是模块启动的核心前置接口
- 响应体大小为 `143 kB`，说明配置文件包含大量模块相关信息，如表单字段、校验规则、选项配置、文案内容等
- 接口响应时间仅 `2 ms`，说明文件被 CDN 或浏览器缓存，加载速度极快
- 该配置文件会为后续的辅修报名表单渲染、提交逻辑提供基础数据支持
1. **前后端分离架构验证**
   模块通过 `Fetch API` 异步加载配置文件，后续表单数据提交也将通过异步请求完成，符合前后端分离的开发模式。
2. **配置化表单设计**
   辅修报名的表单字段、选项、校验规则等均由配置文件下发，前端根据配置动态生成表单，便于后续业务调整。
3. **缓存优化策略**
   配置文件被缓存，加载耗时仅 2ms，有效提升了模块启动速度，优化用户体验。

## 

## 7. 重修报名模块接口分析

### 7.1 基础信息

| 项目   | 说明                             |
|:---- |:------------------------------ |
| 接口名称 | 系统全局配置文件                       |
| 请求方式 | Fetch                          |
| 请求地址 | `/default_config.content.json` |
| 状态码  | 200                            |
| 发起方  | `content_main.js`              |
| 响应大小 | 143 kB                         |
| 耗时   | 1 ms                           |
| 接口用途 | 加载重修报名模块的全局配置、表单规则、业务开关、文案信息等  |

### 7.2 技术分析

- 接口由主业务脚本 `content_main.js` 在模块初始化时通过 Fetch API 异步加载，是重修报名页面启动的核心前置接口
- 响应体大小为 `143 kB`，说明配置文件包含大量模块相关信息，如重修规则、表单字段、校验逻辑、选项配置、提示文案等
- 接口响应时间仅 `1 ms`，说明文件已被浏览器缓存，加载速度极快，可显著提升模块启动速度
- 该配置文件将为后续重修报名表单的渲染、校验与提交逻辑提供基础数据支持
1. **配置化表单设计**
   重修报名的表单字段、选项、校验规则、报名条件等均由配置文件下发，前端根据配置动态生成表单，便于学校后续调整报名规则。
2. **缓存优化策略**
   配置文件被浏览器缓存，加载耗时仅 1ms，有效优化了用户体验，减少了不必要的网络请求。
3. **多模块共用配置体系**
   与辅修报名模块使用同一配置文件接口，说明系统采用统一的配置管理方案，降低了维护成本，保证了各模块的一致性。 

--- 

--- 

## 8. 学生证补办模块接口分析

### 8.1 基础信息

| 项目   | 说明                        |
|:---- |:------------------------- |
| 接口名称 | 学生证补办数据查询                 |
| 请求方式 | XHR（jQuery 发起）            |
| 请求地址 | `/xszbbgl_cxYsqList.html` |
| 请求参数 | `gnmkdm=N10...`（脱敏）       |
| 状态码  | 200                       |
| 发起方  | `jquery.min.js`           |
| 响应大小 | 0.7 kB                    |
| 耗时   | 95 ms                     |
| 接口用途 | 查询学生证补办相关业务数据，如申请记录、审核状态等 |

### 8.2 技术分析

- 接口命名遵循 `业务模块_cx业务缩写.html` 规范，`xszbbgl` 为“学生证补办管理”的拼音缩写，`cxYsqList` 表示“查询申请列表”，符合校园管理系统的典型命名风格
- 请求由 jQuery 发起，响应体仅 0.7 kB，说明返回数据为精简的结构化 JSON 结果，包含申请记录、状态、时间等关键字段
- 接口响应耗时 95ms，属于正常范围，未出现性能瓶颈

## 

## 9.系统全局配置文件接口

### 9.1基础信息

| 项目   | 说明                             |
|:---- |:------------------------------ |
| 接口名称 | 系统全局配置文件                       |
| 请求方式 | Fetch                          |
| 请求地址 | `/default_config.content.json` |
| 状态码  | 200                            |
| 发起方  | `content_main.js`              |
| 响应大小 | 143 kB                         |
| 耗时   | 1 ms                           |
| 接口用途 | 加载学生证补办模块的全局配置、表单规则、业务开关、文案信息等 |

### 9.2 技术分析

- 接口由主业务脚本 `content_main.js` 在模块初始化时通过 Fetch API 异步加载，是学生证补办页面启动的核心前置接口
- 响应体大小为 143 kB，说明配置文件包含大量模块相关信息，如补办规则、表单字段、校验逻辑、选项配置、提示文案等
- 接口响应时间仅 1 ms，说明文件已被浏览器缓存，加载速度极快，可显著提升模块启动速度

### 学生证补办模块整体技术特征

1. **技术栈一致性验证**
   模块同时使用 jQuery 发起数据查询请求，与之前的教务查询模块技术栈保持一致，说明系统在不同业务模块中采用了统一的异步请求方案。
2. **前后端分离架构延续**
   所有数据请求均为异步 XHR/Fetch 请求，页面无刷新加载数据，进一步验证了系统的前后端分离架构。
3. **多模块共用配置体系**
   与辅修报名、重修报名等模块使用同一配置文件接口，说明系统采用统一的配置管理方案，降低了维护成本，保证了各模块的一致性。
4. **模块化设计规范**
   接口命名与参数设计与其他业务模块保持一致，说明系统采用了统一的开发规范，便于后续扩展与维护。

---

---

## 

## 10.课表查询接口

### 10.1 基本信息

| 项目       | 详情                        |
|:-------- |:------------------------- |
| **接口名称** | 学生个人课表查询接口                |
| **请求地址** | `/bjkbdy_cxKbzdxsxx.html` |
| **请求方式** | `GET`                     |
| **所属模块** | 教务管理 / 课表查询               |
| **数据类型** | `XHR`                     |

### 10.2 请求参数

| 参数名      | 示例值       | 说明               |
|:-------- |:--------- |:---------------- |
| `gnmkdm` | `N253508` | 功能模块编码，标识为课表查询业务 |

### 10.3 接口状态

- **Status Code**: `200` (请求成功)
- **响应时间**: `52 ms`
- **数据大小**: `0.9 KB
1. **核心功能**：该接口用于查询学生的个人课表，返回课程名称、上课时间、教室、授课教师等关键信息。
2. **业务逻辑**：
   - 接口依赖当前用户的登录态，无需额外传递学号等参数，通过 Cookie 或 Session 识别用户身份。
   - `gnmkdm` 参数是系统内部的固定模块标识，用于路由到课表查询服务。
3. **返回数据格式**：接口返回 HTML 片段，由前端 JavaScript 解析并渲染成表格形式展开

### Python 请求示例

```python
import requests

# 基础配置
base_url = "你的教务系统域名"
path = "/kbcx_cxKbcxList.html"

# 请求参数
params = {"gnmkdm": "N305007"}
data = {"zd_fzdm": "N305007-xs"}

# 发送请求
response = requests.post(
    url=base_url + path,
    params=params,
    data=data,
    headers={
        "Cookie": "你的登录Cookie",
        "Referer": "课表查询页面地址"
    }
)
```

## 

## 11.空闲教室查询接口

### 11.1 基本信息

| 项目       | 详情                  |
|:-------- |:------------------- |
| **接口名称** | 空闲教室查询接口            |
| **请求地址** | `/cdjy_cxXqjc.html` |
| **请求方式** | `GET`               |
| **所属模块** | 教务管理 / 教室资源查询       |
| **数据类型** | `XHR`               |

### 11.2 请求参数

| 参数名      | 示例值     | 说明                |
|:-------- |:------- |:----------------- |
| `xqh_id` | `00001` | 校区/教学楼编码，用于指定查询范围 |
| `xnm`    | `...`   | 学年参数，标识查询的年份      |
| `xqm`    | `...`   | 学期参数，标识查询的学期      |

### 11.3 接口状态

- **Status Code**: `200` (请求成功)
- **响应时间**: `53-58 ms`
- **数据大小**: `1.1 KB`
1. **核心功能**：该接口用于查询校园内的空闲教室资源，返回教室编号、位置、可使用时间段等信息，是教务系统中教室预约、自习室查询的核心接口。
2. **业务逻辑**：
   - 通过 `xqh_id` 参数指定校区或教学楼，结合学年、学期参数来筛选不同时间段的教室占用情况。
   - 接口返回的数据由前端解析后，渲染为教室状态列表或课表形式，供用户查看和预约。
3. **返回数据格式**：接口返回结构化的 HTML/文本数据，包含教室信息、占用状态和时间安排，由前端 JavaScript 解析并渲染。

### Python 请求示例

```python
import requests

# 基础配置
base_url = "你的教务系统域名"
path = "/jxcdk_xsJxcdkList.html"

# 请求参数
params = {"gnmkdm": "N305006"}
data = {"zd_fzdm": "N305006-xs"}

# 发送请求
response = requests.post(
    url=base_url + path,
    params=params,
    data=data,
    headers={
        "Cookie": "你的登录Cookie",
        "Referer": "空闲教室查询页面地址"
    }
)
```

## 12. 成绩查询接口分析

### 12.1 基础信息

| 字段        | 内容                     |
| --------- | ---------------------- |
| **请求URL** | `zdpz_cxZdpzList.html` |
| **请求方法**  | `POST`                 |
| **请求类型**  | `XHR`                  |
| **状态码**   | `200`                  |

### 12.2 请求参数

#### a.URL Query 参数

| 参数名      | 参数值       | 说明              |
| -------- | --------- | --------------- |
| `gnmkdm` | `N305005` | 功能模块ID，标识成绩查询功能 |

#### b.Form Data 参数

| 参数名       | 参数值          | 说明             |
| --------- | ------------ | -------------- |
| `zd_fzdm` | `N305005-xs` | 业务功能编码，学生端成绩查询 |

### 12.3 接口说明

- **`zdpz_cxZdpzList.html`**：成绩查询列表接口，伪静态格式
- **`gnmkdm`**：系统固定功能模块编码
- **`zd_fzdm`**：业务细分编码，用于区分查询身份/类型
- 接口依赖登录 Cookie，属于学校 CAS 教务系统标准接口

### Python 请求示例

```python
import requests

# 基础配置
base_url = "你的教务系统域名"
path = "/xspj_cxXspjIndex.html"

# 请求参数（需补充实际表单参数）
params = {"doType": "query"}
data = {
    # 从DevTools的Form Data中复制完整参数
}

# 发送请求
response = requests.post(
    url=base_url + path,
    params=params,
    data=data,
    headers={
        "Cookie": "你的登录Cookie",
        "Referer": "学生评价页面地址"
    }
)
```

## 13.学生评价接口分析

### 13.1 基础信息

| 字段        | 内容                      |
| --------- | ----------------------- |
| **请求URL** | `xspj_cxXspjIndex.html` |
| **请求方法**  | `POST`                  |
| **请求类型**  | `XHR`                   |
| **状态码**   | `200`                   |

### 13.2 请求参数

#### a.URL Query 参数

| 参数名      | 参数值       | 说明              |
| -------- | --------- | --------------- |
| `gnmkdm` | `N401605` | 功能模块ID，标识学生评价功能 |

#### b.Form Data 参数

| 参数名       | 参数值          | 说明             |
| --------- | ------------ | -------------- |
| `zd_fzdm` | `N401605-xs` | 业务功能编码，学生端评价查询 |

### 13.3 接口说明

- **`xspj_cxXspjIndex.html`**：学生评价查询接口，伪静态格式
- **`gnmkdm`**：系统固定功能模块编码
- **`zd_fzdm`**：业务细分编码，用于区分查询身份/类型
- 接口依赖登录 Cookie，属于学校 CAS 教务系统标准接口

### Python 请求示例

```python
import requests

# 基础配置
base_url = "你的教务系统域名"
path = "/zdpz_cxZdpzList.html"

# 请求参数
params = {"gnmkdm": "N305005"}
data = {"zd_fzdm": "N305005-xs"}

# 发送请求
response = requests.post(
    url=base_url + path,
    params=params,
    data=data,
    headers={
        "Cookie": "你的登录Cookie",
        "Referer": "成绩查询页面地址"
    }
)
```

## 

## 14.其他学术成果认定接口分析

### 14.1基础信息

| 字段        | 内容                                   |
| --------- | ------------------------------------ |
| **请求URL** | `qtxscgrdsq_cxQtxscgrdsqlIndex.html` |
| **请求方法**  | `POST`                               |
| **请求类型**  | `XHR`                                |
| **状态码**   | `200`                                |

### 14.3 请求参数

#### a.URL Query 参数

| 参数名      | 参数值       | 说明                |
| -------- | --------- | ----------------- |
| `doType` | `query`   | 操作类型，标识为查询请求      |
| `gnmkdm` | `N532565` | 功能模块ID，标识学术成果认定功能 |

#### b.Form Data 参数

| 参数名                      | 参数值             | 说明             |
| ------------------------ | --------------- | -------------- |
| `xqm`                    | `3`             | 学期/学年维度参数      |
| `xnm`                    | `2018`          | 年份参数（如入学/成果年份） |
| `_search`                | `false`         | 搜索模式标识         |
| `nd`                     | `1778230490234` | 时间戳/防缓存随机数     |
| `queryModel.showCount`   | `15`            | 每页显示条数         |
| `queryModel.currentPage` | `1`             | 当前页码           |
| `queryModel.sortName`    | （空）             | 排序字段           |
| `queryModel.sortOrder`   | `asc`           | 排序方式（升序）       |
| `time`                   | `0`             | 时间参数/备用字段      |

### 14.4 接口说明

- **`qtxscgrdsq_cxQtxscgrdsqlIndex.html`**：其他学术成果认定列表查询接口，伪静态格式
- `qtxscgrdsq`：其他学术成果认定的拼音缩写
- `gnmkdm=N532565`：系统固定功能模块编码
- 接口依赖登录 Cookie，属于学校 CAS 教务系统标准接口

### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/qtxscgrdsq_cxQtxscgrdsqlIndex.html"

# 请求参数

params = {
 "doType": "query",
 "gnmkdm": "N532565"
}
data = {
 "xqm": "3",
 "xnm": "2018",
 "_search": "false",
 "nd": "1778230490234",
 "queryModel.showCount": "15",
 "queryModel.currentPage": "1",
 "queryModel.sortName": "",
 "queryModel.sortOrder": "asc",
 "time": "0"
}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "学术成果认定页面地址"
 }
)

###
```

## 15. 答辩信息接口分析

### 15.1 基础信息

| 字段        | 内容                          |
| --------- | --------------------------- |
| **请求URL** | `dbxxck_cxDbxxckIndex.html` |
| **请求方法**  | `POST`                      |
| **请求类型**  | `XHR`                       |
| **状态码**   | `200`                       |

### 15.5 请求参数

#### a.URL Query 参数

| 参数名      | 参数值                | 说明                |
| -------- | ------------------ | ----------------- |
| `doType` | `query`            | 操作类型，标识为查询请求      |
| `gnmkdm` | （待补充，可在Payload中查看） | 功能模块ID，标识答辩信息查询功能 |

#### b. Form Data 参数

| 参数名   | 参数值   | 说明                               |
| ----- | ----- | -------------------------------- |
| （待补充） | （待补充） | 可在浏览器DevTools的Payload标签页查看完整表单参数 |

### 15.4 接口说明

- **`dbxxck_cxDbxxckIndex.html`**：答辩信息查询接口，伪静态格式
- `dbxxck`：答辩信息查询的拼音缩写
- `gnmkdm`：系统固定功能模块编码，标识答辩相关功能
- 接口依赖登录 Cookie，属于学校 CAS 教务系统标准接口

### Python 请求示例

```python
import requests

# 基础配置

base_url = "你的教务系统域名"
path = "/dbxxck_cxDbxxckIndex.html"

# 请求参数（需补充实际的gnmkdm和表单参数）

params = {
 "doType": "query",
 "gnmkdm": "（从Payload中复制的实际值）"
}
data = {

# 从DevTools的Form Data中复制完整参数

}

# 发送请求

response = requests.post(
 url=base_url + path,
 params=params,
 data=data,
 headers={
 "Cookie": "你的登录Cookie",
 "Referer": "答辩信息查询页面地址"
 }
)
```

# 
